<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.project.dao.rwLikeDAO">

    <select id="likeCheck" resultType="int">
        SELECT COUNT(*)
        FROM rw_like
        WHERE m_no = #{param1}
          AND r_no = #{param2}
    </select>

    <insert id="likeInsert">
        INSERT INTO rw_like (rw_no, m_no, r_no)
        VALUES (rw_like_seq.nextval, #{param1}, #{param2})
    </insert>

    <delete id="likeDelete">
        DELETE FROM rw_like
        WHERE m_no = #{param1}
          AND r_no = #{param2}
    </delete>

    <select id="likeCount" resultType="int">
        SELECT COUNT(*) FROM rw_like WHERE r_no = #{param1}
    </select>

</mapper>
